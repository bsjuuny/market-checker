# 🔔 Market Checker (전 세계 휴장일 감시) 개발자 가이드

안녕하세요! 이 프로젝트는 전체 매매 시스템에서 아주 중요한 **'On/Off 스위치'**입니다. 오늘 한국이나 미국 시장이 쉬는지(공휴일) 체크해서 다른 프로젝트들이 헛수고하지 않게 알려주는 역할을 합니다.

---

## 🏗 시스템 작동 방식

1.  **휴장일 체크**: KRX(한국)와 NYSE/NASDAQ(미국) 일정을 확인해서 "오늘 장 열리나?"를 판단합니다.
2.  **데이터 저장**: 상태 정보를 `docs/` 폴더 내에 저장합니다. 
3.  **시스템 연동**: `KIS-Trader` 같은 프로젝트들은 실행 전 이 `Market Checker`의 결과를 먼저 보고, 휴장일이면 일을 하지 않고 잠을 잡니다.

---

## 🛠 주요 명령어 및 작업 가이드

- `npm run dev`: 지금 시장이 어떤 상태(Open/Closed)인지 로컬에서 즉시 확인하고 싶을 때 실행하세요.
- `npm run build:deploy`: 상태를 확인한 뒤 그 결과를 GitHub Pages에 업데이트합니다. 

---

## 💡 개발자를 위한 팁

### 휴장일이 틀릴 경우
- 대체휴일이나 임시공휴일이 갑자기 생겼을 땐 `src/main.js`의 휴장 리스트를 직접 업데이트해야 할 수 있습니다. (주식 시스템 전체가 꼬이지 않게 가장 먼저 확인해야 할 부분입니다!)

### GitHub Pages 연동
- 결과물은 `docs/` 폴더에 쌓이며, `GitHub Pages` 설정에 따라 웹으로 공유됩니다.

---

## 🚨 문제 해결 (Troubleshooting)

**Q: 오늘 장 여는 날인데 봇이 휴장이라고 해요.**
- A: 수집 로직이 공휴일 데이터를 잘못 해석했거나, API가 옛날 정보를 줬을 수 있습니다. `src/main.js`의 스크레이퍼 로직을 디버깅해 보세요.

---

## 📁 주요 구성 요소
- `src/scrapers/`: 각 거래소 사이트에서 일정을 가져오는 스크레이퍼 모음.
- `docs/`: 실제 웹으로 서비스되는 상태 페이지 결과물.
- `.env.local`: 알림 전송을 위한 텔레그램 토큰 설정.
