{
  "name": "market-checker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node src/main.js",
    "build:deploy": "npm test && npm run dev && git add docs/index.html && git commit -m \"docs: automated market update\" && git push origin main",
    "test": "node --test"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "cheerio": "^1.2.0",
    "dotenv": "^17.3.1",
    "iconv-lite": "^0.7.2"
  }
}
